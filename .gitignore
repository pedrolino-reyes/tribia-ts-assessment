# Node modules
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript cache and build output
*.tsbuildinfo
dist/

# Environment variable files (keep these out of source control if they contain sensitive data)
.env
.env.local
.env.*.local

# Playwright test output (if you generate reports or screenshots)
test-results/
playwright-report/

# Optional: Coverage reports (if you use a coverage tool)
coverage/

# Optional: VS Code settings (if you prefer not to share them)
.vscode/

# OS-specific files
.DS_Store
Thumbs.db
